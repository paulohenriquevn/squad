# Design sign-off — {{SCOPE}}

> Generated by `/design`, ALWAYS unticked. The agent that drew the diagrams may never
> tick a box, remove one, or delete this section: doing so fabricates a judgement
> nobody made.
>
> **Sign as `human/{your-name}`.** The prefix is the kit's vocabulary for "a person
> gave this", read by `squad.signoff` and therefore by every gate in the kit. A bare
> name is refused — including an agent's — and so is a deleted checkbox: an empty
> section has nothing unticked in it, which is not the same as reviewed.
>
> These boxes are what the gate reports as NOT checked. `check_design_completeness.py`
> counts drawings and resolves ids; whether the lifecycle is the real one is the
> judgement this signature is for.

## Sign-off

- [ ] D1 — the lifecycle is the real one, and the irreversible transitions are marked
- [ ] D2 — the trust boundary is where untrusted code actually stops
- [ ] D3 — the sequence shows the failure paths, not only the happy one
- [ ] D4 — what survives a process death, a node restart and a deploy is what is drawn
- [ ] D5 — every PIECE-N has a place in the map, and the map is derived from D1–D4

<!-- signed-by: -->
<!-- e.g. human/paulo, or human/paulo (reviewed in the 2026-09-20 session) -->
