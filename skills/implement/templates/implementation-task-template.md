# Implementation: {Title}

> **Generated by `/implement {plan-slug}`** — this is the halt-loop's working contract derived from `.claude/records/plans/{slug}-plan.md`.

**Slug:** `{slug}`
**Source plan:** `.claude/records/plans/{slug}-plan.md`
**Plan verdict:** `{SHIPPABLE / SHIPPABLE_WITH_CAVEATS}` (score `{N}`)
**Working branch:** `workspace` (Unbreakable Rule 4 — work is born on `workspace`, promoted to `develop` via PR; `main` is release-only)
**Started:** `{YYYY-MM-DD}`

## Pre-condition audit (verified at /implement start)

- [ ] Plan verdict ≥ SHIPPABLE_WITH_CAVEATS
- [ ] On `workspace` (current branch: `{branch}`)
- [ ] No uncommitted changes at start
- [ ] Dependencies installed for every language this item touches — `npm install`,
      `go mod download`, `pip install -e .`, `cargo fetch`. N/A only where the
      language is absent from the repository, which is a fact about the manifest.
- [ ] External dependencies up (Postgres if integration tests need it)

## Task list (derived from plan; ordered by dependency)

Each task below MUST complete its TDD cycle (RED → GREEN → REFACTOR → WIRING → COMMIT) before the next task starts. Dependencies enforce ordering.

| # | Plan ref | Status | Wiring (a) | Wiring (b) | Wiring (c) | Commit SHA |
|---|---|---|---|---|---|---|
| T1.1 | Phase 1 / T1.1 | pending | — | — | — | — |
| T1.2 | Phase 1 / T1.2 | pending | — | — | — | — |
| T2.1 | Phase 2 / T2.1 | pending | — | — | — | — |

Status legend — five in flight, two TERMINAL: `pending` / `red` / `green` / `refactor` / `wired` → `committed` or `blocked`. Those last two are the loop's exit condition; any other value leaves the task pending forever.

Wiring legend: `✓` = pillar passed; `✗` = pillar failed; `defer` = ADR-deferred; `—` = not yet checked.

## Per-task contract

Each task below MUST honor the cycle-implement.md hard gates. The halt-loop fills the details progressively as it iterates.

### T1.1 — {Task Title}

**Plan reference:** `.claude/records/plans/{slug}-plan.md` § Phase 1 / T1.1

**Files to edit:** (from plan)

```
src/path/to/file.ts — what changes
src/path/to/file.test.ts — RED test added first
```

**TDD checkpoints:**

- [ ] **RED:** test name `{test_name}` — failing assertion verified
- [ ] **GREEN:** minimal code added; test passes
- [ ] **REFACTOR:** SOLID + Clean Code + DRY review applied; tests stay green
- [ ] **WIRING (a):** static caller — `grep -rl '{symbol}' src/ --exclude='*test*'` returns ≥1 file
- [ ] **WIRING (b):** integration test — `grep -rl '{symbol}' tests/integration/` returns ≥1 file OR ADR-defer
- [ ] **WIRING (c):** runtime metric — `.wiring-evidence.json` shows `{metric}: count > 0` OR plan declared no metric for this task
- [ ] **COMMIT:** `feat({scope}): {description}` (conventional-commits format) with commit body carrying `Plan: {slug}` and `T1.1: {reference}`

**Wiring evidence:**

```
(filled by /implement after wiring check)
Static callers:
  - src/path/caller.ts:42 — invokes {symbol} as part of {flow}

Integration tests:
  - tests/integration/foo.test.ts:8 — exercises {symbol} via {scenario}

Runtime metrics observed:
  - {metric_name}: {count} during {scenario}
```

**Blocked reason (if status=blocked):**

```
(filled by /implement when a task is blocked)
Reason: {Fase A retry exhausted / dependency missing / pattern contradicted}
Recommended human action: {specific next step}
```

### T1.2 — ...

(same shape; halt-loop fills as it iterates)

## Acceptance Criteria (mirrors plan's Global DoD)

- [ ] All non-blocked tasks have status=`committed` with valid SHA
- [ ] Wiring triad: 100% of pillar (a) passes (non-negotiable); pillar (b) ≥ 90% OR explicit ADR for deferred; pillar (c) ≥ 100% of declared metrics observed
- [ ] The project's own suite exits 0, per language present. The validation gate runs
      `go test`, `pytest`, `cargo test` and `npm test` — naming only npm here read a
      Go workspace as having nothing to run.
- [ ] Typecheck exits 0 — `go build ./...`, `cargo check`, `tsc --noEmit`
- [ ] Lint exits 0 on the files THIS change wrote. A tree carries lint debt older than
      the item, and the gate reports it without charging the item for it.
- [ ] Coverage ≥ 90% on changed files (100% on critical paths declared in plan)
- [ ] CHANGELOG.md `[Unreleased]` updated with each task that introduces user-facing change (per Unbreakable Rule 6)

## Halt-loop audit trail

Iteration log (filled by ralph-loop):

| Iteration | Task | Phase | Outcome | Notes |
|---|---|---|---|---|
| 1 | T1.1 | RED | confirmed-fail | test fails as expected |
| 2 | T1.1 | GREEN | pass | minimal impl |
| 3 | T1.1 | REFACTOR | applied | extracted helper |
| 4 | T1.1 | WIRING (a) | pass | 2 callers found |
| 5 | T1.1 | WIRING (b) | pass | 1 integration test added |
| 6 | T1.1 | WIRING (c) | defer | no metric declared |
| 7 | T1.1 | COMMIT | done | SHA abc123 |
| 8 | T1.2 | RED | ... | ... |

## Followups (scope-creep avoided during this implementation)

Items observed during implementation but DEFERRED to a future task/plan to avoid scope creep:

| # | Observed during | Description | Recommended owner |
|---|---|---|---|
| F1 | T1.1 REFACTOR | `helperFn` could be extracted to `src/utils/` if used by another module | next plan |

Save to `.claude/records/implementations/{slug}-followups.md` if it grows beyond 3 entries.

## Validation report

After the halt-loop emits `<promise>IMPLEMENTATION_COMPLETE</promise>` (or exhausts), `/implement` runs `scripts/run_validation.py` and writes the validation report at `.claude/records/reviews/{slug}-implement-validate-{date}.md`.

Final implementation handoff to `cycle-review` happens after that report PASSes.
