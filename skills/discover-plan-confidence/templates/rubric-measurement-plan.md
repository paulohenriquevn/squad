# Rubric Measurement-Plan v1 — M2 Structural

This file is the **source of truth** for the M2 structural rubric for discovery PLANS produced by `/discover-plan`. It defines:

1. The 4 DAG nodes (3 hard-cap dimensions + 1 penalty-only smell dimension) with weights summing to 1.0.
2. The spec-smell detection patterns (dictionaries + regexes), shared with `discover-confidence`'s rubric-blueprint and `plan-confidence`'s rubric-v1.
3. Hard cap values for failure actions.

**v1.0 (2026-05-30):** Initial rubric, copy-with-attribution from `discover-confidence/templates/rubric-blueprint.md`. Adapted for **measurement plans** (output of `/discover-plan`), not blueprints (output of `/discover-execute`). The structural targets differ in node 3:

| Discover-confidence (blueprints) | This rubric (plans) |
|---|---|
| `blueprint_completeness` — all mandatory blueprint sections populated | `plan_completeness` — all 10 mandatory plan sections present + ADR count ≥ 2 + question budget respected + method per question |

Other nodes (research_coverage, reference_citations, structural_risk) are identical in shape to the sibling rubric.

## How to Evolve

When a new check is added (e.g., M3 citation faithfulness — does the cited path actually contain the claimed symbol?), DO NOT edit this file. Create `rubric-measurement-plan-v2.md` with the additional nodes and version 2.

## YAML Definition

```yaml
version: 1
created_at: 2026-05-30
updated_at: 2026-05-30
artifact_type: discover_plan

nodes:
  - id: 1
    dimension: corner_coverage
    description: "All 4 corners covered: evidence, constraint, blast_radius, verification. Each corner has at least one Measurement Question OR an explicit <!-- DEFER-CORNER: {corner} | {reason} --> marker."
    detector: corner_coverage
    failure_action: hard_cap
    hard_cap_value: 49
    weight: 0.30
  - id: 2
    dimension: measurement_targets
    description: "Every path target named in the plan resolves on disk (file OR directory — the plan points at what it will open, so no line number is expected yet). Every live URL is declared in rules/live-target.txt. No fabricated targets."
    detector: measurement_targets
    failure_action: hard_cap
    hard_cap_value: 49
    weight: 0.30
  - id: 3
    dimension: plan_completeness
    description: "All mandatory sections present (Header, Item, Repo, Mode, Context, Hypothesis, Falsification, Measurement Questions, Halt-loop Checkpoints, Acceptance Criteria); the Falsification section states what result kills the hypothesis; total questions in [3, 10] with min 1 per corner (or DEFER-CORNER); Tool AND Target present on every Q-row."
    detector: plan_completeness
    failure_action: hard_cap
    hard_cap_value: 70
    weight: 0.25
  - id: 4
    dimension: structural_risk
    description: "Spec smells below threshold (subjective adjs, weak imperatives, vague pronouns, loopholes, non-verifiable terms)"
    detector: spec_smells
    failure_action: penalty
    penalty_weights:
      subjective_adjectives: -2
      weak_imperatives: -3
      vague_pronouns: -2
      loopholes: -3
      non_verifiable: -4
    weight: 0.15

smells:
  subjective_adjectives:
    pattern_type: dictionary
    words:
      - fast
      - efficient
      - robust
      - scalable
      - optimal
      - smart
      - friendly
  weak_imperatives:
    pattern_type: regex
    pattern: '\b(should|could|may|might)\b'
  vague_pronouns:
    pattern_type: regex
    pattern: '(^|\. )(This|That|These|Those)\b'
  loopholes:
    pattern_type: dictionary
    phrases:
      - "if possible"
      - "as appropriate"
      - "when applicable"
      - "where feasible"
  non_verifiable:
    pattern_type: dictionary
    words:
      - user-friendly
      - maintainable
      - scalable
      - robust
```

## Soft caps (conservative bias)

Beyond the hard caps above, soft caps may fire when signals indicate risk:

- `soft_floor_smell_density_high` — ≥ 20 weak-imperative / loophole / vague hits in prose (cap at 89).
- `soft_floor_citation_density_low` — < 1 `.claude/records/references/` citation per 200 words of plan prose (cap at 89).

Soft caps appear in `hard_caps_triggered` with prefix `soft_floor_` but do NOT trigger `verdict == INVALID`.

## Limitations (Known)

- **M3 future** — citation faithfulness (does the cited path actually contain the claimed behavior?) requires SAFE-adapted semantic check. M2 only verifies path existence.
- **Four corners, and no fifth.** An earlier limitation recorded here promised a `prior_art` corner and asked human reviewers to cover it in the meantime. Both halves are void: `cycle-discover.md § The four corners` declares four, and prior art is now an **anti-pattern** in the same rule — *"Project X does it this way is not a measurement of our system... it cannot fill the Evidence corner."* The paragraph also named `check_research_coverage.py`, which was renamed to `check_corner_coverage.py` when the corners stopped being about other people's projects and started being about ours. Left as a note rather than deleted, because a reader who remembers the fifth corner should find out here that it was retired rather than wonder whether the scorer forgot it.
- **EC analog** — smells inside `## Anti-patterns`, `## ADRs § Anti-pattern`, or `## Halt-loop Checkpoints` sections are counted (false positives in metalanguage). Acceptable trade-off for M2.
- **Regex `\b` boundaries** — ASCII-oriented. Implementation uses `re.UNICODE` for safety.
